<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rxandroid_sample1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.Polling_Activity"></activity>
        <activity android:name=".activities.AsyncTask_Activity" />
        <activity android:name=".activities.Timer_Sample_Activity" />
        <activity android:name=".activities.RecyclerView_sample_Activity" />
        <activity android:name=".activities.Search_Activity" />
        <activity android:name=".activities.Onclick_Activity" />
        <activity android:name=".activities.Loop_Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.HelloActivityV1" />
    </application>

</manifest>